<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    tools:parentTag="androidx.constraintlayout.widget.ConstraintLayout">

    <Button
        android:id="@+id/widget_loading_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Entrar" />

    <ProgressBar
        android:id="@+id/widget_loading_button_progress_bar"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_gravity="center"
        android:elevation="4dp"
        android:indeterminateTint="@android:color/white"
        android:padding="4dp"
        android:progressDrawable="@android:color/white"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@+id/widget_loading_button"
        app:layout_constraintEnd_toEndOf="@+id/widget_loading_button"
        app:layout_constraintStart_toStartOf="@+id/widget_loading_button"
        app:layout_constraintTop_toTopOf="@+id/widget_loading_button"
        tools:visibility="visible" />
</merge>
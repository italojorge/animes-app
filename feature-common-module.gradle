apply from: "../common-android-module.gradle"

android {
    buildFeatures  {
        viewBinding true
    }
}

dependencies {
    featureDependencies.each { add(it.configuration, it.dependency, it.options) }
    presentationDependencies.each { add(it.configuration, it.dependency, it.options) }
}